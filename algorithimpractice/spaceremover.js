//return a string with all the spaces removed.
//input string with spaces.
//output string with spaces removed
//can do this via regex.

//take the string and eliminate the spaces
//split the string to an array, join it without the spaces.

var spaceRemover = function(string){
    return string.split(" ").join("");
}

